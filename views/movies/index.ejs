
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Movies App</title>
    <link rel="stylesheet" href="../../public/css/style.css" />
  </head>
  <body style="margin:0; font-family:Arial, sans-serif;">

    <nav style="background:#222; color:#fff; padding:1rem;">
      <a href="/" style="color:#fff; margin-right:1rem; text-decoration:none;">Home</a>
      <a href="/movies" style="color:#fff; margin-right:1rem; text-decoration:none;">Movies</a>
      <a href="/register" style="color:#fff; margin-right:1rem; text-decoration:none;">Register</a>
      <a href="/login" style="color:#fff; margin-right:1rem; text-decoration:none;">Login</a>
    </nav>

    <main style="padding:2rem;">

  
  <h1>Movies</h1>

  <% if (user) { %>
    <a href="/movies/new" class="btn">Add Movie</a>
  <% } else { %>
    <p><a href="/login">Login</a> to add movies.</p>
  <% } %>

  <ul>
    <% movies.forEach(m => { %>
      <li>
        <a href="/movies/<%= m._id %>"><%= m.name %> (<%= m.year %>)</a>
        - Rating: <%= m.rating %>/10
      </li>
    <% }) %>
  </ul>


    </body>
</html>

