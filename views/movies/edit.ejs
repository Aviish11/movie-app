<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Movies App</title>
    <link rel="stylesheet" href="../../public/css/style.css" />
  </head>
  <body style="margin:0; font-family:Arial, sans-serif;">

    <nav style="background:#222; color:#fff; padding:1rem;">
      <a href="/" style="color:#fff; margin-right:1rem; text-decoration:none;">Home</a>
      <a href="/movies" style="color:#fff; margin-right:1rem; text-decoration:none;">Movies</a>
      <a href="/register" style="color:#fff; margin-right:1rem; text-decoration:none;">Register</a>
      <a href="/login" style="color:#fff; margin-right:1rem; text-decoration:none;">Login</a>
    </nav>

    <main style="padding:2rem;">


  <h1>Edit Movie</h1>

  <form action="/movies/<%= movie._id %>?_method=PUT" method="POST">
    <label>Name</label>
    <input type="text" name="name" value="<%= movie.name %>" required>

    <label>Description</label>
    <textarea name="description" required><%= movie.description %></textarea>

    <label>Year</label>
    <input type="number" name="year" value="<%= movie.year %>" required>

    <label>Genres (comma separated)</label>
    <input type="text" name="genres" value="<%= movie.genres.join(', ') %>" required>

    <label>Rating (0–10)</label>
    <input type="number" step="0.1" name="rating" value="<%= movie.rating %>" required>

    <label>Poster URL (optional)</label>
    <input type="url" name="posterUrl" value="<%= movie.posterUrl %>">

    <button type="submit">Update</button>
  </form>

  </body>
</html>
